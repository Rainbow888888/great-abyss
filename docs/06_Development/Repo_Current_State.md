# Repo Current State

> Обновляется после каждого крупного изменения. Одна страница.

## Сделано
- Пакет документации: концепция, мир, ресурсы, выборы, концовки, летописец, roadmap, тикеты, ADR по движку.
- Три HTML/JS прототипа ядра (вне этого репозитория, для проверки ощущений от механики):
  - «Живой лес» — одноэкранный инкрементал, визуальный рост дерева по стадиям.
  - «Великая Бездна: два экрана» — независимые светлая/тёмная кампании с переключением.
  - «Бездна: Свет и Тьма» — единый экран, поделённый пополам, общий гейдж Бездны, три концовки (черновая калибровка чисел).
- T0001: пустой проект Godot 4 (`project.godot`), папки `/game /art /audio`, репозиторий инициализирован и запушен на GitHub как публичный (`great-abyss`).
- T0002: `game/scenes/Main.tscn` — `Node2D` "Main" с дочерним `Polygon2D` "Bezdna" (круг по центру экрана, тёмный цвет), без логики и без клика.
- T0003: `game/scenes/Main.gd` + «Источник» в `Main.tscn` — `Area2D` слева от Бездны, клик даёт +1 материал (переменная `material_count`, debug-label «Материал: N» и печать в консоль).
- T0004: Грухр (`Polygon2D` "Gruhr", зелёный кружок) рядом с Источником. Клик по Источнику запускает `Tween`-прыжок Грухра (вверх на 20px и обратно с отскоком), без звука и без AI-передвижения.
- T0005: кнопка «Бросить» (`Button` "ThrowButton"). Тратит весь накопленный материал разом, Бездна коротко пульсирует (`scale` 1.0 → 1.08 → 1.0). Первый замкнутый цикл: клик → материал → бросок → отклик Бездны.
- T0006: второй, пассивный Грухр (`Polygon2D` "GruhrPassive", тёмно-бирюзовый) + `Timer` "PassiveTimer" (3 сек). Раз в 3 секунды сам добавляет +1 материал и подпрыгивает — без клика игрока. Подтверждено человеком.
- T0007: интервал и величина прироста пассивного Грухра вынесены в ресурс `game/resources/PassiveGruhrStats.tres` (Data-Driven). Чистый рефакторинг — поведение не изменилось, проверено тем же логом.
- T0007.5: материал стал физическим объектом (`game/scenes/Material.tscn`). Клик по Источнику и тик пассивного Грухра роняют фигуру на землю (`GROUND_Y = 470`) с разбросом по x. Счётчик не растёт, label переименован в «В Бездне», кнопка «Бросить» временно бездействует — оживёт в T0007.6.

- T0007.6: сцена переведена в разрез (ADR-003). Линия поверхности `SURFACE_Y = 280`, под ней массив земли `Zemlya`, Бездна — полость с зевом на поверхности, уходящая за нижний край экрана. Источник и оба Грухра стоят на поверхности.

- T0007.7: замкнут базовый цикл. Грухр-носильщик подбирает лежащий объект, несёт к левой кромке зева (`THROW_X = 590`) и сбрасывает — счётчик растёт в момент броска, Бездна пульсирует. «Источник» переименован в «Шахту» (первый источник засыпания, `docs/03_Gameplay/FillSources.md`) и увеличен до 80×72. Кнопка «Бросить» удалена за ненадобностью.

- T0007.8: уровень заполнения. `Polygon2D` "Zasypka" внутри Бездны, форма строится из контура самой полости, поднимается по мере брошенного. Ёмкость — в `game/resources/AbyssStats.tres` (`capacity = 60`).

- Скорость носильщика замедлена вдвое (220 → 110 px/с) и вынесена в `game/resources/GruhrStats.tres`. Пропускная способность упала ровно вдвое: 6 объектов за 30 секунд вместо 12. Грухри должны ковылять; ускорение — будущий апгрейд, см. `docs/02_World/LightCivilization.md`.

- T0008: сохранение. Два числа (брошено в Бездну, лежит на поверхности) в JSON в `user://save.json`, поле `version` для отбрасывания несовместимых сейвов. Запись при закрытии окна, чтение при старте. Битый файл, чужая версия, отсутствие файла — новая игра без падения, проверено автотестом.

- T0008.5: «Шахта» переименована в **Священный Монолит (Первокамень)**. Визуал — каменная глыба (тёмно-серый многоугольник, 6 точек) вместо жёлтой будки-прямоугольника. Пассивный Грухр стал «молитвенником» и стоит у Монолита. Логика цикла не менялась: клик по Монолиту и молитва роняют осколки, носильщик несёт их к Бездне. Концепция зафиксирована: Светлые выбивают материал верой и молитвой, Тёмные — механизмами (`docs/02_World/LightCivilization.md`, `docs/03_Gameplay/FillSources.md`).

- T0009: автосейв. Таймер `AutosaveTimer` в сцене, каждые 10 секунд — тот же `save_game()`. Интервал — константа `AUTOSAVE_INTERVAL` в `Main.gd` (технический параметр, не баланс). Запись при закрытии остаётся. Проверено автотестом: файл пишется на ~10-й секунде без закрытия окна, данные совпадают со сценой.

- T0010: Летописец, первая запись. Скрытый `Label` "ChronicleLabel" внизу по центру; при первом броске в Бездну появляется строка «Сегодня в Бездну впервые что-то упало. Она не поблагодарила.» (тон — `docs/04_Content/Chronicle.md`). Без ротации, базы событий и сохранения. Проверено автотестом в оконном режиме.

- T0011: записи Летописца вынесены в ресурс `game/resources/ChronicleEntries.tres` (Data-Driven). Три вехи: первое подношение, 10-й бросок, половина ёмкости (порог считается от `AbyssStats.tres`). `Main.gd` только показывает первую сработавшую запись. Добавление записи — правка `.tres`, без кода. Проверено автотестом в оконном режиме.

## В работе
- T0007.5–T0011: код готов, ждут проверки человеком.
- T0008.5: код готов, ждёт проверки человеком (скриншот `tools/shot_t0008_5.png`).

## Изменилось с прошлой версии
- Масштаб сцены уменьшен: Грухры (радиус 20 → 8), Монолит (72 → 36 по высоте, коллизия 80×72 → 40×36), осколки материала (12 → 6). Бездна не тронута — контраст «огромная Бездна / крошечные человечки». Скорость носильщика 110 → 36.7 px/с (втрое медленнее). В коде следом уменьшены: GROUND_Y, разброс кучи (38–105 → 16–44), высота переноски (28 → 12), прыжок (20 → 8). Расстояние от добытчика до Бездны не менялось. У добытчика (GruhrPassive) появилась большая кирка (древко + лезвие), вдвое больше его — визуальный гэг на фоне крошечного Грухра. Скриншот — `tools/shot_scale.png` (в .gitignore).
- Добавлена кнопка "Создать носильщика" в UI (Main.tscn) — спавнит дополнительный carriers-нода. committed `1088562`.
- VCS-метка: `1088562` — T0006-ish: Add CreateCarrierButton.
- В `docs/01_Vision/HighConcept.md` добавлен раздел «Начало игры»: игра начинается с очень короткой заставки (несколько секунд, пустая сцена и Бездна), добыча — сразу после, без кнопки «Начать».
- Доки сверены с фактическим состоянием: «песок из шахты» → «Монолит (осколки)» в `FillSources.md`; F6 в `Roadmap.md` закрыт (автосейв), F7 помечен как «записи готовы, проверка — T0012»; `Verification.md` получает пометку, что скриншот-автолоад по F12 отложен (T0005.5), работает `tools/capture.gd`.
- Приняты референсы: Gnorp Apologue, Dwarf Eats Mountain, Frostpunk — `docs/05_References/DesignReferences.md`, ссылка добавлена в `AGENTS.md` (пункт 1.1). Главный вывод: ресурс должен быть предметом, а не числом, иначе не появляется вторая система (транспорт), на которой держатся обе референсные игры.
- Из-за этого вставлены T0007.5 и T0007.6 перед сохранением: формат сейва зависит от модели данных, поэтому T0008 не должен идти раньше.
- T0007 завершён: числа пассивного Грухра — в `.tres`-ресурсе, а не в коде.
- Начат рабочий процесс: тикеты делаются в отдельных ветках (`tNNNN-...`), слияние в `master` — по подтверждению.
- В `AGENTS.md` добавлены Правила №8–10 (матрица приоритетов, вертикальный срез, «готово» ≠ «отполировано»); языковое правило переехало под №11. Добавлен `docs/06_Development/Verification.md` — как агент показывает и проверяет визуал через `screenshots/latest.png` вместо ручной пересылки картинок.
- В `Tickets.md` расписан весь оставшийся MVP: T0008–T0009 (сохранение/автосейв, F6), T0010–T0011 (Летописец, F7), T0012 (не код — 5-минутная сессия человеком, ответ на вопрос «хочется ли продолжать»). T0008–T0012 пока намеренно грубые.
- T0005.5 (скриншот по F12) отложен: дублирует уже рабочую самопроверку через `tools/capture.gd`, решает другую задачу (человек показывает скриншот ручной игры) — вернуться к нему, когда возникнет конкретная нужда.

## Что делать следующим
- Человек проверяет T0007.5–T0011 одним прогоном: копится ли куча, читается ли разрез, видно ли что носильщик не успевает, понятно ли без цифр что Бездна заполняется, переживает ли всё это перезапуск, читается ли Монолит как камень, сменяются ли строки Летописца.
- Затем T0012 — проверка ядра: 5 минут игры человеком, ответ на вопрос «хочется ли продолжать?» (Правило №5).
- После T0012 — T0013–T0022 (F8–F11 в Roadmap): давление Бездны, апгрейды, катаклизмы, эскалация эгоизма (новый источник — Жертвенный Дом, устаревание Монолита).

## В работе
- T0013 (реклейм): отложено. Осколки будут всасываться только когда куча станет очень большой — вернётся позже.
- T0014 (визуальная куча): код готов. Осколки ложатся друг на друга с шагом 6 px в радиусе 20 px. Монолит увеличен в 5 раз (180px). Куча растёт вверх визуально. Ждёт проверки человеком.

## Замечено и требует решения
- Носильщик безнадёжно не успевает: за 11 секунд теста доставлено 2 объекта, на поверхности скопилось 39. Узкое место видно отлично, но при текущих числах это уже не «напряжение», а тупик. Числа лежат в `GruhrStats.tres` и `PassiveGruhrStats.tres` — крутятся без кода. Балансировать после того, как человек посмотрит вживую.
