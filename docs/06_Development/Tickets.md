# Тикеты

Каждый тикет — маленький, с чёткой областью изменений. Не добавлять ничего сверх описанного. После выполнения — обновить статус здесь и `Repo_Current_State.md`.

---

### T0001 — Создать структуру проекта
- Пустой проект Godot 4 внутри этого репозитория.
- Папки `/game`, `/art`, `/audio` рядом с `/docs`.
- `AGENTS.md` и документация из `/docs` уже на месте (сделано этим пакетом).

**Статус:** Сделано

---

### T0002 — Бездна на сцене, без логики
- Создать `game/scenes/Main.tscn`.
- Добавить `Node2D`/`Sprite2D` «Бездна» — просто круг по центру экрана, тёмного цвета.
- Никакой логики, никакого клика. Только то, что она есть на сцене.

**Статус:** Сделано

---

### T0003 — Источник материала и клик
- Добавить Area2D «Источник» рядом с Бездной (не заменяет её).
- Клик по Источнику: +1 материал в переменную состояния (пока просто печать в консоль или debug-label, без UI-магазина).
- Не добавлять HP, не добавлять анимации — это следующий тикет.

**Статус:** Сделано

---

### T0004 — Первый Грухр
- Заспавнить один статичный Грухр рядом с Источником (простая фигура).
- При клике по Источнику — Грухр коротко «подпрыгивает» (Tween/scale-анимация).
- Ничего не программировать сверх этого (без звука, без AI-передвижения).

**Статус:** Сделано

---

### T0005 — Бросок материала в Бездну
- Материал, накопленный по T0003, тратится вручную (например, кнопка «бросить») — Бездна реагирует визуально (лёгкая пульсация/изменение размера).
- Это первое замыкание цикла: клик → материал → бросок → видимый отклик Бездны.

**Статус:** Ждёт
